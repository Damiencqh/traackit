import 'package:flutter/material.dart';

/// Traackit's palette — warm paper, moss accent.
/// Pulled directly from the HTML prototype so the iOS app matches.
class AppColors {
  AppColors._();

  static const paper = Color(0xFFF4F0E8);
  static const paperWarm = Color(0xFFECE6D8);
  static const card = Color(0xFFFFFFFF);

  static const ink = Color(0xFF1C1A17);
  static const inkMuted = Color(0xFF6E6862);
  static const inkSoft = Color(0xFFA8A29A);

  static const accent = Color(0xFF4F6B3F);
  static const accentSoft = Color(0xFF7C8E6A);

  static const line = Color(0xFFD9D2C2);
  static const lineSoft = Color(0xFFE8E2D5);
}
